package com.sdm.core.ui;

public enum UIInputType {
    text, textarea, password, number, email, url, tel, color, file, image, range, month, week, weekday, date, datetime, time, hidden, checkbox, radio, combo, list, map, autocomplete, object, objectlist, checkList;
}
